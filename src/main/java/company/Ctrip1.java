package company;

import java.util.List;

/**
 * 实现获取下一个排列的函数，算法需要将给定数字序列重新排列成字典序中下一个更大的排列。
 * 如果不存在下一个更大的排列，则将数字重新排列成最小的排列（即升序排列）。必须原地修改，只允许使用额外常数空间。
 *
 * 示例1:
 *
 * 输入：nums = [1,2,4,5,3]
 * 输出：nums = [1,2,5,3,4]
 *
 * 示例2:
 *
 * 输入：nums = [5,4,3,2,1]
 * 输出：nums = [1,2,3,4,5]
 */
public class Ctrip1 {

    public List<Integer> nextPailie(List<Integer> arr) {
//        List<Integer> resList = new ArrayList<>();
//        if (arr == null || arr.size() == 0) {
//            return resList;
//        }
//        int idx;
//        for(idx = arr.size() - 1; idx > 0; idx--) {
//            if (arr.get(idx - 1) > arr.get(idx)) {
//                continue;
//            } else {
//                break;
//            }
//        }
//        if (idx == 0) {
//            resList = reverse(arr, 0, );
//            return resList;
//        }
//
//        swap(arr, idx - 1, idx);
//
//        reverse(arr, 0, );
        return null;
    }
}
